<!DOCTYPE html>
<html>
<head>
  <title>SkiNyc</title>
	<script type="text/javascript"
      src="https://maps.googleapis.com/maps/api/js?key=<%= ENV['GOOGLE_API_KEY'] %>">
    </script>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <%= image_tag "nyski_logo.png" %>
  </header>
  <nav>
    <ul class="nav-navbar">
      <% if !current_user %>
        <li><a href="users/new">Login</a></li>
      <% else %>
      <li><%= link_to "Your Profile", user_path(current_user.id) %></li>
      <% end %>
      <li><a href="/">Find a Mountain Near You!</a></li>
      <li><a href="/trips">Explore Trips!</a></li>
      <li><a href="/trips/new">Create Your Own Trip!</a></li>
    </ul>
  </nav>

	<% if current_user %>
		<%= current_user.email %>
	<% end %>
  <%= yield %>
	<% if current_user %>
		<%= link_to 'Log-Out', login_path, method: :delete %>
	<% end %>
</body>
</html>
